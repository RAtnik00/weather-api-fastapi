[project]
name = "weather-api-fastapi"
version = "0.1.0"
description = ""
authors = [
    {name = "RAtnik00"}
]
readme = "README.md"
requires-python = "=3.14"
dependencies = [
    "skip (>=0.1.0,<0.2.0)",
    "fastapi (>=0.128.7,<0.129.0)",
    "uvicorn (>=0.40.0,<0.41.0)",
    "python-dotenv (>=1.2.1,<2.0.0)",
    "httpx (>=0.28.1,<0.29.0)",
    "pydantic-settings (>=2.13.1,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.0.2,<10.0.0)"
]
